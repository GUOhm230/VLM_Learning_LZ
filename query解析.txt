当前query解析有两个问题：
解析化query--主要主体-属性进行比较
1. 多个属性和值怎么解决
关键是有多个属性和值，而且这些属性和值与官方设定并不一致。
方案1：
主体可以匹配上：
属性要是能对应上：计算query对应属性值和caption对应属性值bge和gte计算相似度。取bge阈值满足条件，则gte的值计算在内
全部属性能对应上，取query属性值-caption主体属性值相似度加权取平均
属性要是对不上，将对不上的属性值分别和query对应主体其他属性值进行相似度计算，并乘以惩罚项0.9

主体无法
使用主体属性拼接的老方式进行计算并施加惩罚项: 0.85
